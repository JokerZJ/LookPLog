name = "lookplog"
main = "workers/spa-fallback.js"
compatibility_date = "2024-11-12"

# 静态资源；SPA 回退由 Worker 处理（避免 _redirects / not_found_handling 触发 100324）
[assets]
directory = "./dist"
binding = "ASSETS"
